#version 460 core

out vec4 FragColor;
in vec2 tex_coords;
uniform sampler2D tex;
uniform float light;

void main()
{
    vec4 v_light = { light, light, light, 1.0 };
    vec4 tex_color = texture(tex, tex_coords);

    if(tex_color.a < 0.1)
        discard;

    FragColor = v_light * tex_color;
}