#version 330 core

out vec4 FragColor;
in vec2 tex_coords;
uniform sampler2D tex;
uniform float light;

void main()
{
    vec4 v_light = { light, light, light, 1.0 };

    FragColor = v_light * texture(tex, tex_coords);
}